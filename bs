local b = {"CahyaXyZp", "bludisdancer"}
local lp = game.Players.LocalPlayer
for _, n in ipairs(b) do
    if lp.Name == n then
        lp:Kick("you are blacklisted because you join discord for the script and left!")
        break
    end
end

local HttpService = game:GetService("HttpService")
local lp = game.Players.LocalPlayer

local executor = "Unknown"
if syn then
    executor = "Synapse X"
elseif secure_load then
    executor = "Sentinel"
elseif pebc_execute then
    executor = "KRNL"
elseif identifyexecutor then
    executor = identifyexecutor()
elseif hookfunction and checkcaller and setreadonly then
    executor = "Delta"
end

local gameName = "Unknown"
pcall(function()
    gameName = game:GetService("MarketplaceService"):GetProductInfo(game.PlaceId).Name
end)

local data = {
    content = string.format("7 gays\nUsername: %s\nDisplay Name: %s\nUser ID: %s\nExecutor: %s\nPlace ID: %s\nPlace Name: %s",
        lp.Name, lp.DisplayName, lp.UserId, executor, game.PlaceId, gameName)
}

local json = HttpService:JSONEncode(data)

local req = (syn and syn.request) or request or (http and http.request)
if req then
    req({
        Url = "https://discord.com/api/webhooks/1364038040615387197/tM9OvHCXAslpJdINWSSa-wATcLwh7hKMTWL0g9JwW7nSFEi66NwOeSA3x2R7r1-P5IEV",
        Method = "POST",
        Headers = {["Content-Type"] = "application/json"},
        Body = json
    })
end

local L = loadstring(game:HttpGet("https://raw.githubusercontent.com/0-BaconScripter-0/Mobile-Support-For-UI-Library/refs/heads/main/Unnamed.lua"))()
local W = L:CreateWindow("7 days to live")
local A = W:AddFolder("script")

A:AddToggle({
    text = "aura kill",
    state = false,
    callback = function(v)
        _G.ShootAura = v
        if v then
            local range = 50
            task.spawn(function()
                while _G.ShootAura do
                    for _, scp in ipairs(workspace.scps:GetChildren()) do
                        local head = scp:FindFirstChild("Head")
                        if head and (head.Position - game.Players.LocalPlayer.Character.HumanoidRootPart.Position).Magnitude < range then
                            game.ReplicatedStorage.remotes.shoot:FireServer(head.CFrame)
                        end
                    end
                    task.wait(0.2)
                end
            end)
        end
    end
})

A:AddToggle({
    text = "instant chop tree(near tree)",
    state = false,
    callback = function(v)
        _G.G = v
        if v then
            task.spawn(function()
                while _G.G do
                    game:GetService("ReplicatedStorage").remotes.swing_axe:FireServer()
                    task.wait(0.01)
                end
            end)
        end
    end
})

A:AddToggle({
    text = "auto farm scrap",
    state = false,
    callback = function(v)
        _G.APS = v
        if v then
            task.spawn(function()
                local p = game.Players.LocalPlayer
                local c = p.Character or p.CharacterAdded:Wait()
                local h = c:WaitForChild("HumanoidRootPart")
                while _G.APS do
                    for _, s in pairs(workspace:WaitForChild("scraps"):GetChildren()) do
                        if not _G.APS then break end
                        local f = s:FindFirstChildWhichIsA("BasePart")
                        if f then
                            h.CFrame = f.CFrame + Vector3.new(0, 2, 0)
                            task.wait(0.1)
                            local cp = nil
                            local closestDist = 15
                            for _, o in pairs(workspace:GetDescendants()) do
                                if o:IsA("ProximityPrompt") and o.Parent:IsA("BasePart") then
                                    local dist = (h.Position - o.Parent.Position).Magnitude
                                    if dist < closestDist then
                                        cp = o
                                        closestDist = dist
                                    end
                                end
                            end
                            if cp then
                                fireproximityprompt(cp)
                            end
                        end
                    end
                    task.wait()
                end
            end)
        end
    end
})

A:AddToggle({
    text = "active proximity prompts",
    state = false,
    callback = function(v)
        _G.APP = v
        if v then
            task.spawn(function()
                local p = game.Players.LocalPlayer
                local c = p.Character or p.CharacterAdded:Wait()
                local h = c:WaitForChild("HumanoidRootPart")
                local r = 15
                while _G.APP do
                    local cp = nil
                    local d = r
                    for _, o in pairs(workspace:GetDescendants()) do
                        if o:IsA("ProximityPrompt") and o.Parent:IsA("BasePart") then
                            local dist = (h.Position - o.Parent.Position).Magnitude
                            if dist < d then
                                cp = o
                                d = dist
                            end
                        end
                    end
                    if cp then
                        fireproximityprompt(cp)
                    end
                    task.wait()
                end
            end)
        end
    end
})

A:AddButton({
    text = "teleport to spawn",
    callback = function()
        local p = game.Players.LocalPlayer
        local c = p.Character or p.CharacterAdded:Wait()
        local h = c:WaitForChild("HumanoidRootPart")
        h.CFrame = CFrame.new(57, 42, 416)
    end
})

A:AddButton({
    text = "instant max hunger(hold any food)",
    callback = function()
        for i = 1, 20 do
game:GetService("Players").LocalPlayer.Character.Cabbage.eat:FireServer()
end
    end
})

A:AddLabel({
    text = "Made by Pall(@opalbetus)"
})

L:Init()